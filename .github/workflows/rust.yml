name: 🦀 Running Rust

on: [push, pull_request]

env:
  CARGO_TERM_COLOR: always

jobs:
  Linter:
    runs-on: ubuntu-latest
    name: 🧹 Linter
    steps:
      - name: 🚀 actions/checkout@v2
        uses: actions/checkout@v2
      - name: 🕵️ Run linter
        run: cargo clippy
  MacOS:
    name: 🍎 MacOS
    runs-on: macos-latest
    steps:
      - name: 🚀 actions/checkout@v2
        uses: actions/checkout@v2

      - name: ⬇ Install youtube-dl
        run: |
          wget https://github.com/ytdl-org/youtube-dl/releases/latest/download/youtube-dl -O youtube-dl
          chmod a+rx youtube-dl
          echo "$(pwd)" >> $GITHUB_PATH

      - name: ⬇️ Install ffmpeg
        run: |
          wget https://evermeet.cx/ffmpeg/get/zip -qO ffmpeg.zip
          unzip ffmpeg.zip

      - name: 🕵️ Run tests
        run: make test
  Ubuntu:
    runs-on: ubuntu-latest
    name: 🐧 Ubuntu
    steps:
      - name: 🚀 actions/checkout@v2
        uses: actions/checkout@v2

      - name: ⬇️ Install youtube-dl
        run: |
          wget https://github.com/ytdl-org/youtube-dl/releases/latest/download/youtube-dl -O youtube-dl
          chmod a+rx youtube-dl
          echo "$(pwd)" >> $GITHUB_PATH

      - name: ⬇️ Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: 🕵️ Run tests
        run: make test
  # todo: https://github.com/TGotwig/vidmerger/issues/37
  # Windows:
  #   runs-on: windows-latest
  #   name: 🏳️‍🌈 Windows
  #   steps:
  #     - name: 🚀 actions/checkout@v2
  #       uses: actions/checkout@v2

  #     - name: ⬇️ Install chocolatey
  #       uses: crazy-max/ghaction-chocolatey@v1
  #       with:
  #         args: -h
  #     - name: ⬇️ Install nmake
  #       uses: ilammy/msvc-dev-cmd@v1

  #     - name: ⬇️ Install youtube-dl
  #       run: choco install youtube-dl

  #     - name: ⬇️ Install ffmpeg
  #       run: choco install ffmpeg

  #     - name: 🕵️ Run tests
  #       run: nmake test
